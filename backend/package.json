{
  "name": "deadlineiq-backend",
  "version": "1.0.0",
  "private": true,
  "description": "DeadlineIQ Lambda handlers and shared libraries",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "dev:api": "node scripts/devserver.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1112.0",
    "@aws-sdk/s3-request-presigner": "^3.1112.0",
    "chrono-node": "^2.10.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1111.0",
    "@aws-sdk/lib-dynamodb": "^3.1111.0"
  }
}
